package plugin

import com.sk89q.worldedit.extent.clipboard.io.ClipboardFormats


class IslandGenerator {
    fun configurator(x: Int, y: Int, z: Int) {
        //grab next empty assignment in database
        //Link that assignment to player
        //player is assigned island, island is generated
        //player is dropped onto island
        //randomly pick an island ID and assign to player
        // check if island is occupied and or grab available range(store in var if generated row by row?)



    }
    fun generator(x: Int, y: Int, z: Int) {
        val format = ClipboardFormats.findByFile(SkyBlockOG.plugin.dataFolder)


    }
}